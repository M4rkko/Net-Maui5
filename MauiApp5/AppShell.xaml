<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiApp5.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiApp5.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="MauiApp5">

    <!-- tab navigation bar -->
    <TabBar>
        <Tab Title="Home" Icon="Home.png">
            <ShellContent Title="Home"
                ContentTemplate="{DataTemplate local:HomePage}" Route="HomePage" >
            </ShellContent>
        </Tab>
        <Tab Title="Create" Icon="Create.png">
            <ShellContent Title="Create"
                ContentTemplate="{DataTemplate local:FlashcardsPage}" Route="FlashcardsPage" >
            </ShellContent>
        </Tab>
        <Tab Title="Play btn" Icon="Play.png">
            <!-- teha prst õigeks kui olemas play funct-->
            <ShellContent Title="Create"
                ContentTemplate="{DataTemplate local:FlashcardsPage}" Route="FlashcardsPage" >
            </ShellContent>
        </Tab>
        <Tab Title="Settings" Icon="Settings.png">
            <!-- teha prst õigkes kui olemas settings-->
            <ShellContent Title="Settings"
                ContentTemplate="{DataTemplate local:NewPage5}" Route="NewPage5" >
            </ShellContent>
        </Tab>
    </TabBar>
</Shell>
